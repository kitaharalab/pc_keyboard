こちらはModelServerで読み込むためのSavedModel形式で保存された学習済みモデルです。
LSTMを使用し、ノード数128、エポック数1000で学習したものです。

SavedModelとして保存されたモデルは次のような構成になっています

・assets

・variables
  
-variables.data-00000-of-00001
  
-variables.index

・keras_metadata.pb

・saved_model.pb

assetsというフォルダには何もありません。
saved_model.pbというファイルがSavedModelを読み込む際に必要となるので、任意のフォルダを作成し、saved_model.pbを作成したフォルダに入れるようにしてください。
